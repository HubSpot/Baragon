<nav class="navbar navbar-default" role="navigation">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navigation-links">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{appRoot}}/">
                {{title}}
            </a>
        </div>

        <div class="collapse navbar-collapse" id="navigation-links">
            <ul class="nav navbar-nav">
                <li {{#ifEqual fragment "status"}}class="active"{{/ifEqual}}>
                    <a href="{{appRoot}}/">Status</a>
                </li>
                <li {{#ifEqual fragment "services"}}class="active"{{/ifEqual}}>
                    <a href="{{appRoot}}/services">Services</a>
                </li>
                <li {{#ifEqual fragment "groups"}}class="active"{{/ifEqual}}>
                    <a href="{{appRoot}}/groups">Groups</a>
                </li>
                {{#if config.elbEnabled}}
                    <li {{#ifEqual fragment "elbs"}}class="active"{{/ifEqual}}>
                        <a href="{{appRoot}}/elbs">Elbs</a>
                    </li>
                {{/if}}
            </ul>
            {{#if config.allowEdit}}
                {{#if config.authEnabled}}
                    <ul class="nav navbar-nav navbar-right">
                        <li>
                            <a data-action="disableEdit">Disable Edit</a>
                        </li>
                    </ul>
                {{/if}}
            {{else}}
                <ul class="nav navbar-nav navbar-right">
                    <li>
                        <a data-action="enableEdit">Enable Edit</a>
                    </li>
                </ul>
            {{/if}}
        </div>
    </div>
</nav>
